1、直接运行measure_thickness.exe，等几秒出现界面
2、单击开始，鼠标移动划线，再次点击结束，并自动生产上边界和左右边界（备注：右键单击一次，再按任意键重画）
3、按任意键（推荐空格）开始画下边界，左右均要超出边界（备注：右键单击一次，再按任意键重画）
4、按任意键结束并删除两侧多余的线，并开始计算CVI（这个时间大概几秒）
5、自动跳转下一张
6、结果保存在result.csv

